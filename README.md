# ReactJS Sorting Algorithms Visualisation
A ReactJS application to visualise different sorting algorithms:
* Bubble Sort
* Insertion Sort
* Merge Sort
* Quick Sort
* Heap Sort

## Setup
Install yarn packages
```bash
yarn install
```
Start React App
```bash
yarn start
```
The webpage should be running on [http://localhost:3000](http://localhost:3000).
